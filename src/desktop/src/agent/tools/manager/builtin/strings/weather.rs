//! User-visible description strings for the weather tool family.

pub const GET_WEATHER_DESCRIPTION: &str = "Get current weather conditions and forecast for a location. Geocodes the location via Nominatim and fetches the NWS forecast (~7 days). Optionally filter by a date (YYYY-MM-DD).";

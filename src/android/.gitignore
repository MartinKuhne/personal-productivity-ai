*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Files left in the source tree during a migration, marked so the build
# skips them. They should be removed with `git rm` once the migration is
# committed and reviewed.
*.removed

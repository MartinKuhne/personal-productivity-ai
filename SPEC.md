> **GUARDRAIL**: Do not edit this file directly unless explicitly instructed.
> Any changes to requirements must be reflected in the corresponding implementation code.
> If drift is detected between this spec and the actual code behavior, notify the user immediately.

# FastMD Technical Specification

## Summary
This document specifies the technical requirements and architecture for **FastMD**, a hardware-accelerated, native Windows Markdown viewer. FastMD delivers high-performance filesystem navigation, GFM table layout, hierarchical Table of Contents (ToC) scrolling, real-time filesystem synchronization, and concurrent metadata indexing.

## Background / Context
Markdown document viewers often rely on web engines or Electron shell instances, which impose significant memory and CPU overhead. FastMD addresses this by providing a single native binary using the Rust programming language and the GPU-accelerated `egui` framework, resulting in instant startup, minimal memory footprint, and fluid rendering.

## Requirements

The requirements below have been formatted using the **Easy Approach to Requirements Syntax (EARS)**, utilizing Ubiquitous, Event-Driven (When), State-Driven (While), Unwanted Behavior (If), and Optional Feature (Where) templates.

## Requirements subsections

| Title | Location | Contents |
| --- | --- | --- |
| User Interface Layout & Styling | [`src/app/ui/SPEC.md`](src/app/ui/SPEC.md) | Multi-pane layout, directory tree & context menus, tabbed viewer, inline editor, background process logs, thinking process rendering, tools dialog, toolbar menu, and table layout renderer (`UI-001`..`UI-066`, `TBL-001`..`TBL-051`) |
| Configuration | [`src/app/config/SPEC.md`](src/app/config/SPEC.md) | YAML configuration schema (`config.yaml`), model configuration & cost routing, token limits, default config generation, config arrival bus, and tool group states (`CONFIG-001`..`CONFIG-013`) |
| Markdown Subsystem | [`src/app/markdown/SPEC.md`](src/app/markdown/SPEC.md) | GFM parsing (tables, tasks, footnotes), heading sizing, line-break semantics, bullet list indentation, table layout & viewport guarantees, ToC navigation, and YAML front-matter (`MD-001`..`MD-018`) |
| Local LLM Interface & Agent Core | [`src/agent/SPEC.md`](src/agent/SPEC.md) | OpenAI-compatible client, agent turn loop, lowest-cost model routing, prompt building & context injection, tool execution with safe/unsafe parallelism, cancel/stop handling, and vector search integration (`AGENT-001`..`AGENT-033`) |
| LLM Tools & Browser Automation | [`src/agent/tools/SPEC.md`](src/agent/tools/SPEC.md) | Filesystem & note tools, cursor pagination & 30-min shared cache, CSV database tools, JMAP productivity (email/calendar/contacts), CalDAV/CardDAV, Trello, weather, centralized `ToolManager`, and headless browser automation (`TOOL-001`..`TOOL-047`, `BRWS-001`..`BRWS-008`, `BRWS-SESSION-001`..`007`, `BRWS-CONF-001`..`005`) |
| Model Context Protocol (MCP) | [`src/agent/tools/mcp/SPEC.md`](src/agent/tools/mcp/SPEC.md) | MCP server connections, ping & tool discovery, tool call dispatch, OAuth 2.1 authorization code flow with PKCE (`S256`), loopback redirect server, token storage & refresh, and UI authentication (`MCP-001`..`MCP-021`) |
| Virtual File System & Workspace | [`src/app/workspace/SPEC.md`](src/app/workspace/SPEC.md) | Content library models (`text`/`image`, priority, readonly), virtual path parser & traversal rejection (`..`), System default library (`%APPDATA%/fastmd/system`), prompt conversation logging, and context-menu Skills (`VFS-001`..`VFS-130`) |
| Batch Processing | [`src/app/agent/batch/SPEC.md`](src/app/agent/batch/SPEC.md) | Batch prompt processing dialog, directory & file pattern selection, prompt discovery via `prompt` tag, batch modes (File vs Directory), concurrency control (1–8), and execution lifecycle (`BATCH-001`..`BATCH-014`) |
| Application Logic & Background Pipeline | [`src/app/SPEC.md`](src/app/SPEC.md) | Concurrent workspace indexer pipeline, live file system watcher (notify), PDF discovery & background conversion, Background Process Log ring buffer (10,000 entries), image vision analysis, vector indexing worker, and CLI/deployment targets (`REQ-301`..`REQ-504`) |
| Discord Bot Integration | [`src/app/integrations/discord/SPEC.md`](src/app/integrations/discord/SPEC.md) | Discord Gateway (WebSocket) and REST client, message handling & streaming, slash commands, per-conversation context & TTL, sliding-window rate limiting, and content safety filtering (`DISCORD-001`..`DISCORD-013`) |

## Optional semantic Markdown search

Where the `vector-search` Cargo feature is enabled, FastMD may build a local semantic index of `.md` and `.markdown` files. Indexing and embedding run only on background workers, `.txt` files are excluded, and the `vector_search` agent tool is available for read-only semantic lookup. The index subscribes to file events before the initial scan and does not replay earlier events; progress is reported in the background log window after each 100 processed Markdown files. Embeddings are produced through the configured `embeddings` use-case model (AGENT-005); where no such model is configured, the feature is disabled and the background log reports the reason.

- RFC 2119 Key Words Reference: [ietf.org/rfc/rfc2119.txt](https://www.ietf.org/rfc/rfc2119.txt)
- Egui Documentation: [github.com/ocornut/egui](https://github.com/ocornut/egui)
- Notify Crate API Documentation: [docs.rs/notify](https://docs.rs/notify)
- Rust Standard Library Threading Models: [doc.rust-lang.org/std/thread/](https://doc.rust-lang.org/std/thread/)
